# DCC Round 1A - build_all.bat Hotfix

Fixes the batch command runner that accidentally tried to execute the label text such as `Node version` as a command.

Apply by copying `build_all.bat` into the Drive Cleaner project root and overwriting the previous file.

Then run:

```bat
build_all.bat
```
